npm init
packet .jason dependencies tiene lo instalado
luego de descargar sequelize sequelize init
npx create-react-app
npm start en ambas
npx sequelize db:seed:all
npx sequelize db:seed --seed <seeder-filename>


IDEA:
Que tengan los mismos desafios cada día
Retos diarios, semanales, por años.
Mensaje de error cuando se sube una foto con mal formato o que es muy grande. 
Posts más nuevos hasta arriba

PARA CORRER POR PRIMERA VEZ EL CODIGO

1. Se debe tener Node.js instalado
2. En el root directory (DATOS2) correr:
   docker-compose up
3. Ir a client y correr:
   npm install
   npm start
4. Ir a server y correr:
   npm install
   npm start
   apagar el servidor otra vez
   npx sequelize db:seed:all
   npm start

Una última cosa es crear la carpeta uploads en el server

LUEGO DE HABERLO CORRIDO UNA VEZ PARA VOLVERLO A CORRER:

1. Iniciar el contenedor en docker desktop o con linea de comando (Yo lo hago con desktop)
2. Ir a client y npm start
3. Ir a server y npm start


autocannon -c 2 -d 5 http://localhost:5000

clinic doctor --on-port 'autocannon -c 2 -d 5 http://localhost:5000' -- node index.js

clinic doctor -- node index.js
